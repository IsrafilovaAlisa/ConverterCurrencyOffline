<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="utf.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:utf"
    Shell.FlyoutBehavior="Disabled"
    Title="utf">

    <TabBar>
        <ShellContent
            Title="Главная страница"
            ContentTemplate="{DataTemplate local:Views.MainPage}"
            Route="MainPage"/>
        <ShellContent
            Title="Настройки"
            ContentTemplate="{DataTemplate local:Views.SettingsView}"
            Route="Settings"/>
        <ShellContent
            Title="Журнал покупок"
            ContentTemplate="{DataTemplate local:Views.FramePurchases}"
            Route="LocalPurchases"/>
    </TabBar>

</Shell>
